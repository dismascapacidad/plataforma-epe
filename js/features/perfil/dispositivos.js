/**
 * dispositivos.js
 * Sección "Mis dispositivos": placeholder a propósito. El modelo de cómo
 * se vincula un dispositivo físico a un usuario todavía no está decidido
 * (queda pendiente, ver README) — así que acá no hay datos ni store propio,
 * solo el mensaje de "próximamente" dentro del dashboard.
 *
 * Script clásico. Namespace: EpeDispositivos.
 */

var EpeDispositivos = (function () {
  function init(root) {
    // Nada que inicializar todavía: sección sin lógica real, ver comentario
    // arriba. Esta función existe igual (vacía) para que dashboard.js tenga
    // un solo patrón consistente al llamar init() de cada sección.
  }

  return { init: init };
})();
